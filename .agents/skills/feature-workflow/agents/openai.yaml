interface:
  display_name: "Feature Workflow"
  short_description: "Route small changes directly; manage explicit Features"
  default_prompt: "Use $feature-workflow to choose Direct delivery or manage an explicit Feature without silently creating workflow state."
policy:
  allow_implicit_invocation: true
